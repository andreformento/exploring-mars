package com.formento.exploringmars.model.impl;

import static org.junit.Assert.*;

public class PlanetAreaImplTest {

    // expectedException.expect(BusinessException.class);
    // expectedException.expectMessage(contains("The position is out of the security area. The boundary is (), ()"));


//    when(currentPosition.getX()).thenReturn(1);
//    when(currentPosition.getY()).thenReturn(2);
//    when(newPosition.getX()).thenReturn(2);
//    when(newPosition.getY()).thenReturn(2);

}
